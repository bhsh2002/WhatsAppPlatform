{
    "name": "whatsapp-platform-server",
    "version": "1.0.0",
    "description": "WhatsApp Management Platform Server with Meta Webhooks",
    "main": "server.js",
    "type": "module",
    "scripts": {
        "start": "node start.js",
        "dev": "node --watch server.js",
        "test": "NODE_ENV=test node --test --test-concurrency=1",
        "test:coverage": "NODE_ENV=test c8 --all --include 'routes/**/*.js' --include 'services/**/*.js' --include 'middleware/**/*.js' --include 'db/**/*.js' --reporter=text-summary --check-coverage --statements 51 --branches 66 --functions 71 --lines 51 node --test --test-concurrency=1",
        "setup:local": "node scripts/ensure-local-secrets.js",
        "migrate:credentials": "node scripts/migrate-sensitive-credentials.js",
        "backup": "node scripts/backup-database.js",
        "verify:backup": "node scripts/verify-backup.js",
        "validate:production-env": "node scripts/check-production-env.js"
    },
    "dependencies": {
        "bcryptjs": "^3.0.3",
        "better-sqlite3": "^13.0.3",
        "cors": "^2.8.6",
        "dotenv": "^18.0.1",
        "express": "^5.2.1",
        "express-rate-limit": "^8.7.0",
        "form-data": "^4.0.5",
        "helmet": "^8.3.0",
        "jsonwebtoken": "^9.0.3",
        "multer": "^2.0.2",
        "web-push": "^3.6.7"
    },
    "devDependencies": {
        "c8": "^12.0.0"
    }
}
